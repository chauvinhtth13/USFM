name: "toy_dataset"
type: "SegBase" # xem SEG_DATASETS trong usdsgen/data/datasets.py
num_classes: 2 # 2 for [0,1], 3 for [0,1,2], etc.
img_size: 512
path:
  root: "${data.dataset_path}/Seg/toy_dataset"
  split:
    train: "training_set"
    val: "val_set"
    test: "test_set"
    vis:
      samples: 16
      interval: 10

transform:
